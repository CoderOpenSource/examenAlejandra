<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para acceder a la cámara -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Permiso para leer el almacenamiento externo -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".usuario.presentation.trainer.HomeTrainerActivity" />
        <activity android:name=".usuario.presentation.trainer.RegisterClientActivity" />
        <activity android:name=".usuario.presentation.trainer.UpdateClientActivity" />
        <activity android:name=".usuario.presentation.trainer.ClienteActivity" />
        <activity android:name=".rutina.presentation.CategoriaEjercicioActivity" />
        <activity android:name=".rutina.presentation.CrearCategoriaActivity" />
        <activity android:name=".rutina.presentation.UpdateCategoriaActivity" />
        <activity android:name=".rutina.presentation.ListarEjerciciosActivity" />
        <activity android:name=".rutina.presentation.CrearEjercicioActivity" />
        <activity android:name=".rutina.presentation.UpdateEjercicioActivity" />
        <activity android:name=".rutina.presentation.ListarRutinasActivity" />
        <activity android:name=".rutina.presentation.CrearRutinaActivity" />
        <activity android:name=".rutina.presentation.UpdateRutinaActivity" />
        <activity android:name=".rutina.presentation.AddExerciseToRoutineActivity" />
        <activity android:name=".rutina.presentation.VerEjerciciosRutinaActivity" />
        <activity android:name=".rutina.presentation.ListarRutinasByClientActivity" />
    </application>

</manifest>